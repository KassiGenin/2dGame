package io.github.epitech_game;

/**
 * Enum to represent the direction from which the hero spawns into a screen.
 */
public enum SpawnDirection {
    FROM_TOP,
    FROM_BOTTOM,
    FROM_LEFT,
    FROM_RIGHT,
    CENTER // New direction for initial spawn
}
